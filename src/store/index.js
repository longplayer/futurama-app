import { createStore } from 'vuex'
import * as characters from './modules/characters'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    characters
  }
})
